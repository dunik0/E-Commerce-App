import React, { PropTypes } from 'react';
import NavBar from './NavBar';

const Header = props => {
    return (
        <header>
            <NavBar />
        </header>
    );
};

Header.propTypes = {

};

export default Header;