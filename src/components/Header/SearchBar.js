import React, { PropTypes } from 'react';

const SearchBar = props => {
    return (
        <div>
            <button></button>
            <input type='text' />    
        </div>
    );
};

SearchBar.propTypes = {
    
};

export default SearchBar;